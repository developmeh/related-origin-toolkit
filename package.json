{
  "name": "webauthn-rpid-validation-toolkit",
  "version": "1.0.1",
  "description": "A TypeScript library for validating passkey/WebAuthn origin constraints in .well-known/webauthn endpoints.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build && npm test",
    "dev": "tsc --watch"
  },
  "keywords": [
    "webauthn",
    "passkey",
    "origin",
    "validation",
    "browser-extension",
    "security",
    "well-known"
  ],
  "author": "developmeh",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/developmeh/webauthn-rpid-validation-toolkit.git"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "psl": "^1.9.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}